name: Daily Update

on:
  schedule:
    - cron: '*/10 * * * *'  # Runs every 10 minutes
  workflow_dispatch:  # Allows manual triggering

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3  # Updated to the latest version of checkout
    - name: Set up Python
      uses: actions/setup-python@v4  # Updated to the latest version of setup-python
      with:
        python-version: '3.9'  # Consider pinning to a specific version (e.g., '3.9')
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests PyGithub  # Added PyGithub to the install command
    - name: Run update script
      run: python ai_agent_git.py 
